/**
 * Aetheris Store — Barrel Export
 *
 * Centralised re-export for all Zustand stores.
 * Import stores from here, not from individual files.
 *
 * @example
 * import { useAppStore } from "@/store"
 */

export { useAppStore } from "./app-store";
