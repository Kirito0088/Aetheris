/**
 * Aetheris Services — Barrel Export
 *
 * External service clients (API clients, adapters).
 *
 * Architecture rules (SYSTEM_ARCHITECTURE.md):
 * - Frontend components never communicate with AI providers directly.
 * - AI interactions occur exclusively through the AI Orchestration component.
 * - External service clients live here, not inside feature modules.
 */

// Services will be added here in Phase 2 and Phase 3.
// Example: export { venueService } from "./venue-service";
// Example: export { weatherService } from "./weather-service";
